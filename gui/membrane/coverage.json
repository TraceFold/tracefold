{
  "note": "What the faces declare they call and draw. Membership in the three ledgers is NOT written here -- it is computed from this file against the route table and the field domain. The terminal face (glovrex_app/terminal/, req/946) has landed and is the first entry in `consumed`/`drawn`: its registration below is machine-checked against its own exports (`READS`/`SUBJECT` in terminal/tui.mjs, `COLUMNS` in terminal/roles.mjs) by membrane/test/face_coverage.test.mjs (req/967 §4-2), not hand-guessed -- run that suite after touching either file. As each face lands it adds itself to `consumed`/`drawn`, and its reason below must be removed in the same change or the gate goes red for keeping a stale explanation.",
  "consumed": {
    "get_healthz": ["terminal"],
    "get_transformations": ["terminal"],
    "get_candidates": ["terminal"],
    "get_escalations": ["terminal"]
  },
  "drawn": ["get_transformations.items"],
  "requested": [],
  "reasons": {
    "NOT_CONSUMED": {
      "get_attach_sources": { "tag": "undesigned", "note": "attach-source registry (req/824 A4, crate commit 16863593, absorbed req/822_c6); no face is built" },
      "post_attach_sources": { "tag": "undesigned", "note": "as above; and the membrane's body-less idempotency key would collide two different register bodies -- named in route-table.json's idempotency_source before any face may consume this" },
      "get_attach_sources_id": { "tag": "undesigned", "note": "attach-source registry; no face is built" },
      "post_candidates": { "tag": "undesigned", "note": "no face proposes a change yet" },
      "get_candidates_id": { "tag": "undesigned", "note": "no face is built" },
      "post_candidates_id_verify": { "tag": "undesigned", "note": "asking twice is not ruled on; whether a second verify is a new question is open" },
      "post_candidates_id_commit": { "tag": "undesigned", "note": "no face is built" },
      "post_candidates_id_escalation": { "tag": "undesigned", "note": "no face is built" },
      "post_candidates_id_cancel": { "tag": "undesigned", "note": "no face is built" },
      "post_transformations_id_undo": { "tag": "undesigned", "note": "no face is built" },
      "post_transformations_id_replay": { "tag": "undesigned", "note": "no face is built" },
      "get_transformations_id": { "tag": "undesigned", "note": "no face is built" },
      "get_receipts_tid": { "tag": "undesigned", "note": "no face is built" },
      "get_ledger_proof": { "tag": "unimplemented", "note": "the input to offline re-verification; a face for it needs a verifier this app does not carry" },
      "get_ledger_checkpoint": { "tag": "unimplemented", "note": "as ledger/proof" },
      "get_ledger_consistency": { "tag": "undesigned", "note": "no face is built" },
      "get_stream": { "tag": "unimplemented", "note": "live events; nothing consumes the byte stream the membrane hands back" },
      "post_verdict_checkpoints": { "tag": "undesigned", "note": "no face is built" },
      "get_verdict_checkpoints": { "tag": "undesigned", "note": "no face is built" },
      "get_verdict_checkpoints_window_end": { "tag": "undesigned", "note": "no face is built" }
    },
    "NOT_DRAWN": {
      "get_candidates.items": { "tag": "undesigned", "note": "the terminal face reads this route (`consumed`, via READS) so its denominator counts and its disclosure name it, but the face draws rows only from its declared SUBJECT (get_transformations); whether a second panel ever draws candidates is undesigned" },
      "get_candidates.next_cursor": { "tag": "undesigned", "note": "paging is folded inside the membrane (port.fold); a face may never see this member directly" },
      "get_transformations.next_cursor": { "tag": "undesigned", "note": "as above -- folded internally by port.fold; unlike .items, the cursor itself is never handed to the renderer" },
      "get_escalations.items": { "tag": "undesigned", "note": "as get_candidates.items -- read by the terminal face, not drawn; get_transformations is the only SUBJECT" },
      "get_escalations.next_cursor": { "tag": "undesigned", "note": "as get_candidates.next_cursor -- folded internally" },
      "get_verdict_checkpoints.items": { "tag": "undesigned", "note": "no face is built" },
      "get_verdict_checkpoints.next_cursor": { "tag": "undesigned", "note": "as above" },
      "get_verdict_checkpoints.total": { "tag": "undesigned", "note": "no face is built" },
      "get_ledger_consistency.consistent": { "tag": "undesigned", "note": "no face is built; when one is, this word may not be drawn as offline verification" },
      "get_ledger_consistency.checked_from": { "tag": "undesigned", "note": "no face is built" },
      "get_ledger_consistency.checked_to": { "tag": "undesigned", "note": "no face is built" }
    },
    "NOT_A_ROUTE": {}
  }
}
