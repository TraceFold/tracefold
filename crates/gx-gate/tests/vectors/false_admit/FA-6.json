{
  "id": "FA-6",
  "kind": "false-admit",
  "statement": "a transformation to /etc must not be admitted even at order-2 (a change to the policy itself) (sem: SEM-gx-gate-339)",
  "expected": "MUST_DENY",
  "expected_reason": {
    "source": "Policy",
    "policy_id": "fs-deny-etc",
    "code": "POLICY_DENIED"
  },
  "request": {
    "substrate": "fs",
    "locator": "/etc/passwd",
    "order": 2,
    "invert_available": true,
    "evidence": 0
  },
  "why": "one vector that measures AC-029's 'there is no order-2-only bypass path' from **the shipped pack's answer**, not from the gate's call trace. If a branch that waves order-2 through on sight enters the gate or the mapping, this one vector alone tips onto the admit side while FA-1, on the same locator, stays green. (sem: SEM-gx-gate-340)",
  "spec_anchor": "34 AC-029 / 41 §3(order) / req/63 §2",
  "origin": "gx self-authored for M3 hand 6",
  "control": {
    "request": {
      "substrate": "fs",
      "locator": "/tmp/x",
      "order": 2,
      "invert_available": true,
      "evidence": 0
    },
    "expected": "MUST_ADMIT",
    "note": "**the control that makes D-9 visible**: the shipped pack has nothing that refuses order-2 (no ready-made invariant ships), so an order-2 to /tmp is admitted. The back side of the negative case's claim that the refusal's source is the locator, not the order (sem: SEM-gx-gate-341)"
  }
}
