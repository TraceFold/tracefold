# Funding channel for TraceFold (pay-what-you-want via Stripe).
# Switched from Polar to Stripe on 1 Sep 2026 (Polar checkout account's
# onboarding was not finishing). Stripe has no dedicated FUNDING.yml
# platform key, so this stays under `custom:` same as the prior Polar link.
custom: ["https://buy.stripe.com/8x214mbkU2pu5yneyk97G00"]
